<%- include ('header') %>
    
<div class="container">
    <div class="logo">
        <a href="/">
            <img src="images/logo-branca.png"/>
        </a>
    </div>
    <div class="content">
        <div class="result">
            <% if(error) { %>
                <%= error %>
            <% } else{ %>
                <%= quantidade %> dólares na cotação
                <%= cotacao %> é igual a: <%= conversao %> reais
            <% } %>
            
        </div>
    </div>
   
    
</div>

<%- include ('footer') %>